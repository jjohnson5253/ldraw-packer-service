{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd ~ && wget -q https://library.ldraw.org/library/updates/complete.zip && unzip -q complete.zip && rm complete.zip && npm install"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}